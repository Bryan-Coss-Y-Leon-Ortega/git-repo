This chapter will focus on built-in javascript methods
    Global javascript methods
    String
    Math
    Date
    Array
    Number
    
Decoding and Encoding URI'S
    uri is a uniform resource identifiers
        An identifier of a certain resources
    URL: uniform resource locator, locator and hold the information on how to access it (location).

DecodeUri() and encodeUri()
    Encoding fixes the URI's to prevent spaces and other error causing errors.

Parsing Numbers
    Example of parsing strings into numbers

Math and int methods. 
    There are a lot of math and int methods that are similar to java. this is a good reference, but i already have good practice of it. 
    